# 2026-03-19 作業メモ

## 実施内容
- 各チェーンの現在ガス代を過去 7 日と比較する「ガス代相対指数」の素案と実装計画を作成。
- 計画は `doc/plan/2026-03-19-gas-relative-index.md` に整理し、初期値は 10 分サンプリング、7 日 percentile、SQLite 保存に確定。
- `feature/gas-relative-index` ブランチを作成して実装を開始。
- バックエンドに SQLite 履歴ストアと相対指数計算ロジックを追加し、`/fees/` レスポンスへ `relative_index` を付与。
- API コンテナ内に 10 分間隔のバックグラウンドサンプラを追加し、`shared/history/gas_history.sqlite3` へ standard モードの履歴を保存するようにした。
- React 側テーブルに `1W Relative` 列を追加し、`8/10 高い` / `集計中` を表示するようにした。
- FastAPI の HTML テンプレート `fees.html` にも `1W Relative` 列と `過去7日比 上位xx%` 表示を追加した。
- `web/tsconfig.json` に `noEmit` を追加し、`tsc` が `web/src/` に不要な JS を出力しないよう修正。
- `.gitignore` に `shared/history/*.sqlite3` を追加し、実行時 DB をコミット対象から除外した。
- コンテナを再ビルドし、`/fees/` の JSON と `/fees/?format=html` の両方で相対指数表示を確認した。

## 確認結果
- `.venv/bin/pytest -q api/tests/test_fees.py api/tests/test_relative_index.py api/tests/test_beefy_actions.py`
- `npm test`
- `npm run build`
- `docker compose up --build -d`
- `curl http://localhost:9000/fees/`
- `curl http://localhost:9000/fees/?format=html`

## 気づき
- 実際の運用系データでは Infura 側の `429 Too Many Requests` が断続的に発生するため、サンプラ周期は 10 分初期値が妥当。
- HTML ビューは React とは別経路なので、API レスポンス拡張だけでは UI に出ず、テンプレート改修が必要だった。

## 次のアクション候補
- サンプラの実行ログを残し、RPC 使用量と 429 発生頻度を継続観測する。
- 将来的に `warming_up` の閾値やラベル文言を実データに合わせて調整する。
- 必要なら 10 分 / 15 分の切替を `.env` 経由で操作できるようにする。
